name: hmusic
description: A cross-platform music player application
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.9.34
  just_audio_background: ^0.0.1-beta.15
  flutter_bloc: ^8.1.3
  get_it: ^7.6.0
  injectable: ^2.5.0
  equatable: ^2.0.7
  dartz: ^0.10.1
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  injectable_generator: ^2.6.2
  auto_route_generator: ^7.2.0
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
